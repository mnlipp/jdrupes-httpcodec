buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'biz.aQute.bnd:biz.aQute.bnd.gradle:3.3.0'
    }
}

apply plugin: 'biz.aQute.bnd.builder'

repositories {
    jcenter()
}

dependencies {
    compile 'junit:junit:4.12','biz.aQute.bnd:annotation:2.4.0'
    testCompile 'junit:junit:4.12'
}

// Configure sensible layout
sourceSets {
    main {
        java {
            srcDir 'src'
        }
        resources {
            srcDir 'resources'
        }
    }

    test {
        java {
            srcDir 'test'
        }
        resources {
            srcDir 'resources'
        }
    }
}

jar {
    
    into('META-INF') {
        from 'resources/META-INF'
    }
    
}
